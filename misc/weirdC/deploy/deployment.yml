apiVersion: apps/v1
kind: Deployment
metadata:
  name: weirdC
  labels:
    category: misc
    challenge: weirdC
spec:
  replicas: 2
  selector:
    matchLabels:
      category: misc
      challenge: weirdC
  template:
    metadata:
      labels:
        category: misc
        challenge: weirdC
    spec:
      enableServiceLinks: false
      containers:
      - name: server
        image: gcr.io/glowing-reserve-336013/weirdC
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
          requests:
            cpu: 10m
            memory: 30Mi
        ports:
        - containerPort: 1337
          name: tcp
        livenessProbe:
          tcpSocket:
            port: tcp
          initialDelaySeconds: 3
          periodSeconds: 30